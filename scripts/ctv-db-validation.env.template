# CTV Database Validation - Email Notification Configuration
# Copy this file to /etc/ctv-db-validation.env and fill in your values
# sudo cp scripts/ctv-db-validation.env.template /etc/ctv-db-validation.env
# sudo nano /etc/ctv-db-validation.env

# SMTP Server Configuration
SMTP_SERVER=localhost
SMTP_PORT=25
SMTP_USE_TLS=false

# Email Addresses
SMTP_SENDER_EMAIL=operations@crossingstv.com
SMTP_RECIPIENT_EMAILS=

# Optional: SMTP Authentication (if your mail server requires it)
# SMTP_USERNAME=
# SMTP_PASSWORD=

# Example configurations:
#
# For local mail server:
# SMTP_SERVER=localhost
# SMTP_PORT=25
# SMTP_USE_TLS=false
# SMTP_SENDER_EMAIL=operations@crossingstv.com
# SMTP_RECIPIENT_EMAILS=admin@example.com,team@example.com
#
# For Gmail:
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USE_TLS=true
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_SENDER_EMAIL=your-email@gmail.com
# SMTP_RECIPIENT_EMAILS=admin@example.com
#
# For Office 365 / Outlook:
# SMTP_SERVER=smtp.office365.com
# SMTP_PORT=587
# SMTP_USE_TLS=true
# SMTP_USERNAME=your-email@yourdomain.com
# SMTP_PASSWORD=your-password
# SMTP_SENDER_EMAIL=your-email@yourdomain.com
# SMTP_RECIPIENT_EMAILS=admin@example.com

